name: Vcpkg OpenSSL Windows

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: 🛠️ Install Vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: ${{ runner.workspace }}/vcpkg_root
          # Fixed input name:
          vcpkgGitCommitId: '594ad8871e1e8e45f8e626c015fd611163430207'

      - name: 📦 Install OpenSSL via Vcpkg (x64-windows)
        run: |
          echo "Installing openssl:x64-windows..."
          # Note: vcpkg is automatically added to PATH by the previous step
          vcpkg install openssl:x64-windows
          echo "Installation complete!"

      - name: 🔬 Verify OpenSSL Installation
        run: vcpkg list
        